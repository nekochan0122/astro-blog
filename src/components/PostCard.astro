---
import type { Frontmatter } from '@/types'

const { slug, title, description, minutesRead } = Astro.props as Frontmatter
---

<a
  rel='prefetch'
  href={`/blog/${slug}`}
  class='flex flex-col p-3 sm:p-4 border border-grey-200 dark:border-gray-800 rounded-lg text-gray-600 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-800 transition-colors duration-300'
>
  <span class='flex justify-between items-center mb-1'>
    <h3 class='text-lg md:text-xl text-gray-900 dark:text-gray-100'>{title}</h3>
    <span class='shrink-0'>{minutesRead}</span>
  </span>
  <p class='line-clamp-2'>{description}</p>
</a>
